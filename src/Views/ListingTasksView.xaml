<UserControl x:Class="Task_Manager_GUI.src.Views.ListingTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Task_Manager_GUI.src.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="1"
                Background="#046276"
                RenderOptions.EdgeMode="Aliased" />

        <Border Grid.Column="0"
                Background="#065465"
                RenderOptions.EdgeMode="Aliased" />

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>


            <Grid.Resources>
                <ControlTemplate x:Key="RoundButtonTemplate"
                                 TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="50"
                            Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Viewbox x:Name="plusSymbol"
                                     Visibility="Collapsed"
                                     Stretch="Uniform">
                                <Canvas Width="100"
                                        Height="100"
                                        Background="Transparent">
                                    <Rectangle Width="60"
                                               Height="12"
                                               RadiusX="6"
                                               RadiusY="6"
                                               Fill="{TemplateBinding Foreground}"
                                               Canvas.Left="20"
                                               Canvas.Top="44"
                                               IsHitTestVisible="False" />
                                    <Rectangle Width="12"
                                               Height="60"
                                               RadiusX="6"
                                               RadiusY="6"
                                               Fill="{TemplateBinding Foreground}"
                                               Canvas.Left="44"
                                               Canvas.Top="20"
                                               IsHitTestVisible="False" />
                                </Canvas>
                            </Viewbox>

                            <ContentPresenter x:Name="contentPresenter"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Margin="0 0 0 3" />
                        </Grid>
                    </Border>


                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="border"
                                    Property="Background"
                                    Value="Azure" />
                        </Trigger>

                        <Trigger Property="Content"
                                 Value="">
                            <Setter TargetName="plusSymbol"
                                    Property="Visibility"
                                    Value="Visible" />
                            <Setter TargetName="contentPresenter"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </Trigger>

                        <Trigger Property="Content"
                                 Value="{x:Null}">
                            <Setter TargetName="plusSymbol"
                                    Property="Visibility"
                                    Value="Visible" />
                            <Setter TargetName="contentPresenter"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Grid.Resources>

            <Button x:Name="AddButton"
                    Grid.Row="0"
                    Width="50"
                    Height="50"
                    Margin="10"
                    Background="#d5dcf0"
                    Foreground="Black"
                    Template="{StaticResource RoundButtonTemplate}"
                    FontSize="30"
                    Command="{Binding NavigationCommand}"/>

            <Button x:Name="SettingsButton"
                    Grid.Row="1"
                    Width="50"
                    Height="50"
                    Margin="10"
                    Background="#d5dcf0"
                    Foreground="Black"
                    Template="{StaticResource RoundButtonTemplate}"
                    FontSize="30"
                    VerticalAlignment="Bottom"
                    Content="⚙" />

        </Grid>
    </Grid>
</UserControl>
